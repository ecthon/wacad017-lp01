name: Agendar Hello World

on:
  schedule:
    - cron: "0 19 * * *" # Executa às 19:00 UTC todos os dias

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Manda um Hello
        run: echo "Hello World! Este é um workflow agendado."
